{
  "name": "readlater-tts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/background.ts src/popup.ts src/content/extract.ts --bundle --outdir=dist --format=esm",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "esbuild": "^0.20.0",
    "happy-dom": "^14.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "dompurify": "^3.0.0"
  }
}
